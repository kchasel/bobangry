%html
  %head
    %meta{'http-equiv' => 'refresh', content: "30"}
    %title
      Build
      = @revision
      = @success
    :css
      .project_info {
        color: white;
        font-family: 'Helvetica', 'sans-serif';
        font-size: 30px;
        font-weight: bold;
       }
  %body{style: "margin: 0; padding: 0"}
    - if @failure
      .project_info{style: "text-align: center; width: 100%; height: 100%; background-color: #7A9AD6; font-size: 200px"}
        \:(
        %div{style: "font-size: 40px"}
          (bobanky seems to have gone missing)
    - else
      %table{width: "100%", height: "100%"}
        %tr
        - @projects.each do |project|
          %td{width: "#{100.0/@projects.size.to_f}%",
            valign: "top",
            height: "100%",
            style: "background-color: #{project[:success] ? 'green' : 'red' }; padding: 10px"}
            .project_info
              = project[:name]
              %br
              %span{style: "font-size: 24px"}
                = project[:sha][0..6]
